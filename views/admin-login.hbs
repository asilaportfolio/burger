<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Login - Mix Burger</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
  <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
  <style>
    body {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      padding: 20px;
    }
    
    /* Clipboard Container */
    .clipboard-container {
      position: relative;
      width: 100%;
      max-width: 450px;
    }
    
    /* Clipboard Top (Clip) */
    .clipboard-clip {
      position: absolute;
      top: -40px;
      left: 50%;
      transform: translateX(-50%);
      width: 150px;
      height: 50px;
      background: #4a4a4a;
      border-radius: 20px 20px 0 0;
      box-shadow: 0 -5px 15px rgba(0,0,0,0.2);
      z-index: 2;
    }
    
    .clipboard-clip::before {
      content: '';
      position: absolute;
      top: 12px;
      left: 50%;
      transform: translateX(-50%);
      width: 25px;
      height: 25px;
      background: white;
      border-radius: 50%;
      border: 3px solid #4a4a4a;
    }
    
    /* Clipboard Body */
    .clipboard-body {
      background: white;
      border-radius: 15px;
      box-shadow: 0 20px 60px rgba(0,0,0,0.3);
      padding: 60px 40px 40px;
      position: relative;
      z-index: 1;
    }
    
    /* Form Styles */
    .login-header {
      text-align: center;
      margin-bottom: 30px;
    }
    
    .login-header i {
      font-size: 50px;
      color: #667eea;
      margin-bottom: 15px;
    }
    
    .login-header h2 {
      color: #333;
      font-weight: 700;
      margin-bottom: 5px;
    }
    
    .login-header p {
      color: #666;
      font-size: 14px;
    }
    
    /* Checkbox Style Input */
    .form-group {
      position: relative;
      margin-bottom: 25px;
    }
    
    .checkbox-line {
      display: none;
    }
    
    .form-control {
      border: none;
      border-bottom: 2px solid #e0e0e0;
      border-radius: 0;
      padding: 10px 5px;
      transition: all 0.3s ease;
      flex: 1;
    }
    
    .form-control:focus {
      border-bottom-color: #667eea;
      box-shadow: none;
      background: transparent;
    }
    
    .form-label {
      font-weight: 600;
      color: #555;
      margin-bottom: 10px;
      display: flex;
      align-items: center;
      gap: 8px;
    }
    
    .btn-login {
      width: 100%;
      padding: 14px;
      border-radius: 10px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      border: none;
      color: white;
      font-weight: 600;
      font-size: 16px;
      transition: all 0.3s ease;
      margin-top: 20px;
    }
    
    .btn-login:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 20px rgba(102, 126, 234, 0.4);
    }
    
    .alert {
      border-radius: 10px;
      margin-bottom: 20px;
      border: none;
    }
    
    .back-btn {
      text-align: center;
      margin-top: 20px;
    }
    
    .back-btn a {
      color: #667eea;
      text-decoration: none;
      font-weight: 500;
      transition: all 0.3s ease;
      display: inline-flex;
      align-items: center;
      gap: 5px;
    }
    
    .back-btn a:hover {
      color: #764ba2;
    }

    /* Responsive Design */
    @media (max-width: 576px) {
      .clipboard-body {
        padding: 50px 25px 30px;
      }
      .clipboard-clip {
        width: 120px;
        height: 45px;
      }
      .login-header i {
        font-size: 40px;
      }
      .login-header h2 {
        font-size: 22px;
      }
    }
  </style>
</head>
<body>
  <div class="clipboard-container">
    <!-- Clipboard Clip -->
    <div class="clipboard-clip"></div>
    
    <!-- Clipboard Body -->
    <div class="clipboard-body">
      <div class="login-header">
        <i class='bx bxs-clipboard'></i>
        <h2>Admin Panel</h2>
        <p>Mix Burger boshqaruv tizimi</p>
      </div>

      {{#if error}}
      <div class="alert alert-danger" role="alert">
        <i class="fas fa-exclamation-circle me-2"></i>{{error}}
      </div>
      {{/if}}

      <form action="/admin-login" method="POST">
        <!-- Username Field -->
        <div class="form-group">
          <label class="form-label">
            <i class="fas fa-user"></i>
            Login
          </label>
          <input type="text" class="form-control" name="login" placeholder="Login kiriting" required autocomplete="username">
        </div>

        <!-- Password Field -->
        <div class="form-group">
          <label class="form-label">
            <i class="fas fa-lock"></i>
            Parol
          </label>
          <input type="password" class="form-control" name="password" placeholder="Parol kiriting" required autocomplete="current-password">
        </div>

        <button type="submit" class="btn btn-login">
          <i class="fas fa-sign-in-alt me-2"></i>Kirish
        </button>
      </form>

    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
